{
  "name": "tinkoff-invest_v2_sdk",
  "version": "1.0.1",
  "description": "",
  "main": "tinkoff.v2.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "git submodule update --remote --merge --init --recursive"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.12.2",
    "fast-copy": "^3.0.2",
    "grpc-promise": "^1.4.0"
  }
}
